resources:
  pipelines:
    bronze_competitions_pipeline:
      name: bronze_competitions_pipeline_${bundle.target}

      catalog: ${var.catalog}
      schema: ${var.schema}

      serverless: true
      channel: CURRENT

      libraries:
        - file:
            path: ../pipelines/bronze/01_bronze_competitions.sql